<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!--showAsAction如果有剩余空间就显示出来，否则隐藏-->
    <item android:id="@+id/menu_pk"
        android:enabled="true"
        app:showAsAction="ifRoom"
        android:title="@string/main_tab_anime"
        android:icon="@drawable/ic_main_tab_anime" />
    <item android:id="@+id/menu_home"
        android:enabled="true"
        app:showAsAction="ifRoom"
        android:title="@string/main_tab_movie"
        android:icon="@drawable/ic_main_tab_movie" />
    <item android:id="@+id/menu_mine"
        android:enabled="true"
        app:showAsAction="ifRoom"
        android:title="@string/main_tab_set"
        android:icon="@drawable/ic_main_tab_set" />
</menu>